/*
// WebStorage - localStorage e sessionStorage


// LocalStorage = salva a informação localmente

    var nome = '';

    if(typeof localStorage.nome == 'undefined'){
        localStorage.nome = prompt("Digite seu nome")
    }

    nome = localStorage.nome;

    document.getElementById('nome').innerHTML = nome;

    
// SessionStorage = salva a informação apenas na sessão, se recarregar a pagina perde os dados. O uso é igual ao do localStorage
*/